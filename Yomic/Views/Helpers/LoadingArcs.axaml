<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="100" d:DesignHeight="100"
             x:Class="Yomic.Views.Helpers.LoadingArcs"
             HorizontalAlignment="Center" VerticalAlignment="Center">
    <UserControl.Styles>
        <Style Selector="Arc.spinner">
            <Style.Animations>
                <Animation Duration="0:0:0.9" IterationCount="Infinite">
                    <KeyFrame KeyTime="0:0:0">
                        <Setter Property="RotateTransform.Angle" Value="0"/>
                    </KeyFrame>
                    <KeyFrame KeyTime="0:0:0.9">
                        <Setter Property="RotateTransform.Angle" Value="360"/>
                    </KeyFrame>
                </Animation>
            </Style.Animations>
        </Style>
    </UserControl.Styles>

    <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid Width="48" Height="48">
            <!-- Background Track (Subtle Ring) -->
            <Arc Width="40" Height="40" 
                 Stroke="{DynamicResource PrimaryAccent}" Opacity="0.15" StrokeThickness="3.5" 
                 StartAngle="0" SweepAngle="360" 
                 VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <!-- Active Spinning Arc (Flat Single Arc) -->
            <Arc Classes="spinner" Width="40" Height="40" 
                 Stroke="{DynamicResource PrimaryAccent}" StrokeThickness="3.5" 
                 StartAngle="0" SweepAngle="270" 
                 StrokeLineCap="Round" VerticalAlignment="Center" HorizontalAlignment="Center"
                 RenderTransformOrigin="50%,50%">
                <Arc.RenderTransform>
                    <RotateTransform/>
                </Arc.RenderTransform>
            </Arc>
        </Grid>
    </Viewbox>
</UserControl>
